import * as React from 'react';
import FormContainer from '../form/FormContainer'

const Layout = ({}) => {
  return(
    <main>
      <div>
        Place Image here
      </div>
      <div>
        <FormContainer />
      </div>
    </main>
  )
}

export default Layout;